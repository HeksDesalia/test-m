import { Routes } from '@angular/router';
import { FormFieldContainerComponent } from './form-field-container/form-field-container.component';

export const routes: Routes = [
  { path: '', component: FormFieldContainerComponent },
];
